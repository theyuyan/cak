id: __NAME__
version: 0.1.0
kernelCompat: "^0.3.0"
roles: [capability]
license: Apache-2.0
provides:
  implementations:
    - contract: { name: __CONTRACT__, version: 1.0.0, schemaDigest: "__DIGEST__" }
      priority: 50
entrypoint: { type: subprocess, command: node, args: ["dist/main.js"] }
health: { startupTimeoutMs: 3000, probe: call }
