# 安全政策

## 报告漏洞
请**不要**开公开 issue。发邮件到 security@（组织建立后填写正式地址；建仓前请直接联系维护者）。我们在 3 个工作日内确认，**90 天披露期**（可协商）。

## 范围
内核（句柄验证、账本完整性、边界隔离）、SDK、内置插件、注册表索引与签名。第三方插件的漏洞请报给对应作者；若涉及注册表分发，我们会执行 `plugin.yanked`。

## 我们承诺的安全底线（可在测试里核对）
- 没有句柄就没有路径：`tests/topology`、`tests/hostile`
- 策略后请求不可变：Mutation Boundary 测试
- 账本 hash 链与回执可验：`tests/ledger`
- 插件拿不到内核内部对象：Isolation 测试
