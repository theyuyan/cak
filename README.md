# Composable Agent Kernel — 工作区

设计基线：`docs/design/`（→ `~/Desktop/CAK-v0.3设计包`）。状态：**准备完成，未开工**（见 `docs/PREP_REPORT.md`）。

- 工具链：`docs/TOOLCHAIN.md` · 开发纪律：`docs/DEVELOPMENT_DISCIPLINE.md`
- 一条命令核实全部准备：`npm install && npm test`（或 `npm run prep:all` 看逐项输出）
